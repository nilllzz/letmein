/**
 * Custom JSDoc type definitions.
 * Names need to be unique.
 * VSCode automatically reads this file and the definitions can be used across the whole project.
 */

/**
 * @typedef {{location:string,name:string,icon:string,app:string,content:string,isDir:boolean}} FileSystemFile
 */
